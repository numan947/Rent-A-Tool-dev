/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseDtoToolTransactionDto } from '../../models/page-response-dto-tool-transaction-dto';

export interface FindAllBorrowedTools$Params {
  page?: number;
  size?: number;
}

export function findAllBorrowedTools(http: HttpClient, rootUrl: string, params?: FindAllBorrowedTools$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseDtoToolTransactionDto>> {
  const rb = new RequestBuilder(rootUrl, findAllBorrowedTools.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseDtoToolTransactionDto>;
    })
  );
}

findAllBorrowedTools.PATH = '/tools/borrowed';
